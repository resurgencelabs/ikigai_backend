{
  "name": "token",
  "version": "1.0.0",
  "description": "Secondary contract to test out the Subscription Contract",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "yarn clean && tsc -b",
    "build:dev": "tsc -b --watch",
    "clean": "rm -rf ./dest tsconfig.tsbuildinfo",
    "start": "yarn build && DEBUG='token' node ./dest/index.js"
  },
  "dependencies": {
    "@aztec/aztec.js": "^0.11.1",
    "@aztec/noir-contracts": "^0.11.1",
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2"
  }
}
